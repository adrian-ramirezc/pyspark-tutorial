# Using PySpark to Handling Big Data

PySpark handles the data in a similar way as pandas. 

A model was trained using th ML library of PySpark.

In order tu run the scripts, Spark local installaton is needed. 

